I want to build a highly advanced, enterprise-grade, self-hosted alternative to Beeceptor.com for API mocking, HTTP request interception, real-time debugging, and behavior virtualization.

Architectural & Language Autonomy: You have absolute freedom to choose the modern tech stack (e.g., Go, Rust, Node.js, Bun, Elixir, Fastify, Next.js, Svelte, Redis, Postgres, etc.) that delivers the lowest possible proxy response latency (<10ms overhead), high-volume concurrent streaming, and an reactive real-time dashboard UI. Choose your tools strategically and justify your choices.

Core UX Paradigms:
1. No user registration or password wall. A user enters their email address on the landing page to instantly generate or resume an endpoint session (e.g., `https://[unique-token].mock.local`). The session is securely persisted via browser storage.
2. Fully containerized deployment using Docker and orchestrated via Docker Compose.

Provide a comprehensive, production-grade architectural specification, frontend components, core proxy code, and deployment scripts. Structure your response into the following 6 integrated sections:

---

### SECTION 1: SYSTEM SPECIFICATION & ADVANCED BEECEPTOR FEATURES
Ensure that the architecture, UI forms, and proxy route matching engines explicitly implement and configure the following core features:
1. Stateful / Multi-Step Transactions: Integrate an ephemeral in-memory or Redis key-value store per endpoint. Allow rules to mutate or read a session state string/object (e.g., Request 1 to `/login` sets a state variable `authenticated: true`; Request 2 to `/dashboard` matches a rule only if `authenticated == true`).
2. Instant CRUD Routing Engine: Allow a user to toggle "Enable Auto-CRUD". When enabled, the endpoint automatically acts as a REST database backend. `POST /books` appends a small JSON object to an internal array; `GET /books` lists them; `DELETE /books/:id` removes them, all without writing a single mock rule.
3. Proxy-Based Partial Mocking (Man-in-the-Middle Mode): Allow an endpoint configuration to specify a "Target Real API URL". If an incoming request does not match any user-defined MockRules, the proxy automatically forwards the request to the real API, captures the real response, saves it to the RequestLogs stream, and returns it to the client.
4. Auto-CORS Engine: Every intercepted request must automatically handle pre-flight `OPTIONS` requests, injecting dynamic and wide-open CORS headers (`Access-Control-Allow-Origin: *`, allowed headers, and methods) so frontend web apps never hit blockages.
5. Rich Dynamic Response Templating: Support dynamic replacement tags in mock response bodies (e.g., `{{now 'iso'}}`, `{{random 'uuid'}}`, or echoing back path/query variables like `{{request.query.id}}`).
6. Simulated Network Conditions: Sliders for simulated latency delays (0-10,000ms), rate-limiting/throttling (requests per minute limits), and a percentage dial to inject chaotic network dropouts or random HTTP errors (502, 503, 504).

---

### SECTION 2: ARCHITECTURE & HIGH-DENSITY UI/UX (React + Tailwind)
Build a developer-centric, high-density dashboard interface using React and Tailwind CSS. Provide clean frontend component code for:
1. Streamlined Entry Screen: Minimal landing page with an email submission form that acts as a secure access key.
2. Real-Time Split-Screen Layout:
   - Left Column: An ultra-fast, real-time scrolling vertical feed of incoming HTTP traffic showing color-coded method badges, truncated endpoints, served status codes, and an active, pulsing WebSocket connection health indicator.
   - Right Column (Deep Inspector): Comprehensive multi-tab details of the clicked request: "Headers", "Query Params", "Body" (formatted elegantly with collapsible JSON/XML tree nodes), "Response Served", and a dedicated "State & Tracing Logs" tab tracking which rules matched and state changes.
3. Feature Rich "Create Rule" Form: A clean, multi-tab modal that neatly organizes fields for Matching Criteria, Response Payloads, Response Templating, Webhook Actions, and Throttling parameters.

---

### SECTION 3: SYSTEM ARCHITECTURE & HIGH-THROUGHPUT STORAGE MODELS
1. Tech Stack Justification: Outline your chosen programming languages, proxy engines, and data stores, explaining why they maximize I/O throughput and keep internal processing overhead at a bare minimum.
2. Wildcard DNS & Proxy Routing Architecture: Detail how your routing design completely isolates wildcard interception endpoints (`*.yourdomain.com/any/path`) from standard backend management APIs and frontend web UI assets.
3. Data Retention Lifecycle Policy: Outline an automated background pruning strategy that caps database storage at 100 historical request traces per email endpoint or implements a strict 24-hour Time-To-Live (TTL) rule.
4. Provide the system data schema (SQL or NoSQL depending on your stack selection) that neatly captures Endpoints, MockRules, StateObjects, and RequestLogs.

---

### SECTION 4: THE CORE INTERCEPTOR ENGINE & ROUTING
Provide production-ready, highly clean, strongly-typed server code implementing the core request interception router:
1. Multi-Tenant Request Parsing: A catch-all middleware that extracts the dynamic subdomain, validates it against the active database, handles global auto-CORS pre-flights, and logs metadata.
2. Rules Engine & State Evaluator: The core sequence logic that:
   - Inspects the database for matching path rules, header conditions, and state requirements.
   - Evaluates the custom JSON body engine (injecting random tags or variable echo components).
   - Manages state modifications if a stateful workflow rule triggers.
   - Executes proxy forward routines if Target Proxy mode is active and no local rules match.
   - Latency application & response payload serialization.

---

### SECTION 5: REAL-TIME PIPELINE (WebSockets/SSE)
1. Backend Broadcast Server: Code that immediately intercepts new transactional database traces or Redis events and broadcasts the log instantly over a messaging pipe to web sockets or Server-Sent Events matching that specific active subdomain channel.
2. Resilient Frontend Event Hook (`useRequestStream`): A clean custom React hook managing the lifecycle of the data pipe. It must seamlessly handle dropouts, enforce exponential backoff reconnection algorithms, and feed incoming request logs into the dashboard state securely without lagging or locking up the browser DOM.

---

### SECTION 6: LOCAL TUNNEL CLIENT BLUEPRINT & DOCKER COMPOSE
1. Local Tunnel Architecture: Explain how a developer could download a simple CLI tool, type `mock-tunnel --port 3000 --endpoint my-slug`, and securely route public internet traffic hitting `my-slug.yourdomain.com/` directly back through your server down into their local localhost service (reverse-proxy/tunnel design).
2. Multi-Container Orchestration: Provide complete, functional Dockerfiles (Frontend, Backend Proxy) and a master `docker-compose.yml` file. Map out all databases, caching layers, application logic, required persistent volumes, network channels, and deterministic service health-check readiness states.

Write clean, robust, enterprise-grade code throughout, utilizing comprehensive error-handling frameworks and inline comments explaining complex routing paradigms.
