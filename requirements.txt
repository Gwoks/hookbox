# HookBox — Beeceptor-class API mocking & interception platform
# Locked stack (do not substitute): FastAPI on uvicorn (async) · aiosqlite (WAL)
# · Redis · httpx · server-rendered Jinja2 + HTMX + Alpine.js + Tailwind.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiosqlite>=0.19.0
pydantic>=2.6.0
email-validator>=2.1.0         # EmailStr validation for SessionCreate
python-multipart>=0.0.9
redis>=5.0.0                   # async redis.asyncio: state KV, token bucket, pub/sub
httpx>=0.27.0                  # MITM forward (streaming async client)
jinja2>=3.1.0                  # server-rendered UI templates (FE lane)
itsdangerous>=2.1.0            # signing helpers
websockets>=12.0              # mock-tunnel CLI control channel client
